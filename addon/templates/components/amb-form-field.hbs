{{#with (component inputComponent
          autofocus=autofocus
          classNames=inputClass
          type=type
          value=value
          formattedValue=formattedValue
          convertedOptions=convertedOptions
          prompt=prompt
          min=min
          max=max
          step=step
          placeholder=placeholder
          disabled=disabled
          autocomplete=autocomplete
          name=name
          action=(action 'valueChanged')
          didInteractionComplete=(action 'setInteractionComplete')
        ) as |input|}}
  {{#if hasBlock}}
    {{yield (hash
      input=input
      header=(component 'amb-form-field-header' field=this)
      label=(component 'amb-form-field-label' field=this)
      hint=(component 'amb-form-field-hint' field=this)
      description=(component 'amb-form-field-description' field=this)
      errors=(component 'amb-form-field-errors' field=this)
    )}}
  {{else}}
    {{#with this as |field|}}
      {{partial 'components/amb-form-field-header'}}
      {{component input}}
      {{partial 'components/amb-form-field-description'}}
      {{partial 'components/amb-form-field-errors'}}
    {{/with}}
  {{/if}}
{{/with}}
