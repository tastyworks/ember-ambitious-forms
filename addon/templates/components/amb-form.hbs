<form {{action 'domSubmit' on='submit'}}>
  {{#with (hash insertField=(action 'insertField') removeField=(action 'removeField')) as |actions|}}
    {{yield (hash
      actions=actions
      scope-each=(component 'amb-form-scope-each' afForm=this formActions=actions)
      scope=(component 'amb-form-scope' afForm=this formActions=actions)
      field=(component 'amb-form-field' scope=scope afForm=this onInsert=actions.insertField onRemove=actions.removeField)
    )}}
  {{/with}}
</form>
